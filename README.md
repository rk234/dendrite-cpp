# Dendrite
From scratch neural network implementation in C++.

## Building
Make sure clang/clang++ is installed.

```bash
$ cd dendrite-cpp
$ mkdir build
$ cd build
$ cmake ..
$ cd ..
$ cmake --build build
```

## Running

```bash
$ build/dendrite-cpp
```
